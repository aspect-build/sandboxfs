[package]
name = "sandboxd"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "sandboxfs"
path = "src/main.rs"

[dependencies]
backend = { path = "../backend" }
# Swapped to the git source for release builds; see packaging/package.sh.
backend-cfs = { path = "../backend-cfs" }
metrics = { path = "../metrics" }
libc = "0.2"

[package]
name = "sandboxd"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "sandboxfs"
path = "src/main.rs"

[dependencies]
backend = { path = "../backend" }
# Swapped to the git source for release builds; see packaging/package.sh.
cfs = { path = "../cfs" }
fskit-fs = { path = "../fskit-fs" }
metrics = { path = "../metrics" }
libc = "0.2"

[package]
name = "sandboxd"
version = "0.1.0"
edition = "2021"

[lib]
name = "sandboxd"
path = "src/lib.rs"

[[bin]]
name = "sandboxfs"
path = "src/main.rs"

[dependencies]
backend = { path = "../backend" }
fskit-fs = { path = "../fskit-fs" }
metrics = { path = "../metrics" }
libc = "0.2"
